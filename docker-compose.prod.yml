version: '2'
services:
  web:
    build: ./client
    volumes:
      - web-dist:/code/dist
    environment:
      - ENV=PROD
  api:
    build: ./server
    volumes:
      - uwsgi-socket:/var/run/adventurelookup
    environment:
      - ENV=PROD
      - SECRET_KEY='ONLY-FOR-TESTING-MUST-CHANGE-BEFORE-DEPLOY'
  nginx:
    build: ./nginx
    volumes:
      - ./nginx/conf.prod.d:/etc/nginx/conf.d
      - uwsgi-socket:/var/run/adventurelookup
      - web-dist:/usr/share/nginx/html/adventurelookup
    links:
      - web
    ports:
      - "80:80"
    environment:
      - ENV=PROD
  db:
    image: postgres
    volumes:
      - data:/var/lib/postgresql/data
    environment:
      - ENV=PROD
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=adventurelookup
  redis:
    image: redis

volumes:
  uwsgi-socket:
  web-dist:
  data:
    # external: true
